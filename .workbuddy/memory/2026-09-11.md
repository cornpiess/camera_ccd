# 2026-09-11

## 换机首次拉取仓库（本机工作区建立）

工作区 `C:\Users\zh95\Desktop\camera_ccd` 原本为空，本轮从远端克隆完成。

- 命令：`git clone https://github.com/cornpiess/camera_ccd.git .`（直接克隆进工作区根目录）
- 结果：`main` 分支，HEAD = `5ab9b49`，已追踪 `origin/main`，工作树干净。
- 仓库内含 18 个顶层条目，`.workbuddy/memory/` 随之同步（记忆文件确实在跟踪内）。

### ⚠️ 本机两个待处理项

1. **git 身份未配置**：`git config user.name` / `user.email` 在本机与全局均为空。
   提交前必须先设，否则 commit 会失败或被写成无主提交：
   ```
   git config user.name  "..."
   git config user.email "..."
   ```
2. 依赖尚未安装：`node_modules/` 不存在，需 `npm install` 后 `npm run verify` 才可确认基线为绿。

### 环境备注
- 本机 git = 2.55.0.windows.3。
- **Bash 工具在本机不可用**：shell 的 PATH 缺 `dirname`/`head`，任何 bash 命令直接 127 失败。本机一切命令改用 PowerShell 工具执行。
- PowerShell 工具存在**不捕获 stdout** 的现象（多数命令返回空输出）。可靠做法：命令内 `... | Out-File <临时文件>`，再用 Read 工具读回。
